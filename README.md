## Build Instructions
To compile this, ensure you have a C++ compiler, CMake, and the SFML library installed (libsfml-dev on Ubuntu, or via vcpkg/brew depending on your OS).

1. mkdir build && cd build

2. cmake ..

3. make (or cmake --build . on Windows/MSVC)

4. Run the executable: ./AsteroidShooter